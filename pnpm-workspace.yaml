minimumReleaseAgeExclude:
  - '@next/*'
  - next

# core-js ships a postinstall "donation" banner that is not required for the
# package to work. Explicitly acknowledging it here prevents pnpm from failing
# with ERR_PNPM_IGNORED_BUILDS in strict/CI environments.
ignoredBuiltDependencies:
  - core-js
