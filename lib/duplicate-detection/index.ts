/**
 * SPEC 103 — Duplicate Detection: public surface.
 * ---------------------------------------------------------------------------
 * Import the pure matching engine from "@/lib/duplicate-detection" (browser +
 * server safe) and, on the server only, the DB-backed review/merge workflow
 * from "@/lib/duplicate-detection/service".
 */
export * from "./model"
