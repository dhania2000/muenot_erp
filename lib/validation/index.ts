/**
 * SPEC 102 — Data Validation Engine: public surface.
 * ---------------------------------------------------------------------------
 * Import the pure engine from "@/lib/validation" (browser + server safe) and,
 * on the server only, the DB-backed helpers from "@/lib/validation/service".
 */
export * from "./model"
